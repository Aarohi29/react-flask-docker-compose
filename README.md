# Full-Stack Docker Demo (React + Flask)

This project demonstrates a fully Dockerized full-stack application with:

React (frontend)

Flask (backend API)

Docker & Docker Compose for container orchestration

The two services communicate seamlessly through a shared network created by Docker Compose.

## Run
docker compose up --build

## Access
Frontend → http://localhost:3000  
API → http://localhost:5001/api
